import { Chip, Paper } from '@mui/material';

function EventsTimetable() {

  return (
    <Paper
      sx={{
        display: 'flex',
        justifyContent: 'left',
        flexWrap: 'wrap',
        listStyle: 'none',
        p: 1,
        m: 0,
      }}
      component="ul"
    >
      timetable
    </Paper>
  )
}

export default EventsTimetable